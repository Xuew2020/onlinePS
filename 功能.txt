基础功能（缩放√ --- 会改变图像大小、
		 移动√ -- 鼠标事件、
		 旋转√ --- 会改变图像大小、
		 剪切√ --- 会改变图像大小、
		 滤镜√、画笔√ 、马赛克√、橡皮擦√
		 尺子√、拾色器√、抠图√、文字工具√、绘图工具√）

历史记录√

图层 -- 多个canvas 
        --选择 、新建√、删除、透明度设置√、设置canvas大小√
文字工具√ -- 新图层
裁切：抠图√、剪切√、钢笔工具(直线、曲线）
橡皮擦√ -- 依赖新图层 -- 删除局部像素
调色：修改RGBA(滤镜)
拾色器√
污点修复（待选）


水印（离屏canvas）√

ps中常用功能
框选、移动√、裁剪√、图像大小√（分像素数、分辨率）、图像模式（RGB、CMYK、索引模式等）、
吸管√（取色用）、印章（防制用）、魔棒（选取相同或相近颜色）、画笔√（必要时直接画）、
套索（框选任意形状区域）、钢笔（绘制任意形状矢量图形或选区）、
蒙板（为了保留插画原图）--- 可模拟、对比度、亮度√、饱和度、色阶或自动色阶、
文字√、滤镜√、图层样式等等。